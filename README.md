# svelte-max

Svelte 5 + SvelteKit 2 course project. See `COURSE.md` for the full curriculum.

## Quickstart

```bash
pnpm install
pnpm dev
```

## Stack

- Svelte 5.55+, SvelteKit 2.55+
- TypeScript strict mode
- PE7 CSS architecture (OKLCH, `@layer`, fluid `clamp()`, mobile-first)
- pnpm only
